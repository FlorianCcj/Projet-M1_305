/**
 * Created by Florian on 16/02/2015.$
 * Je sais pas ce que c'est donc je le garde
 */
/*
NanScope();
libusb_device * * devs;
int
cnt = libusb_get_device_list(usb_context, & devs
)
;
CHECK_USB(cnt);

Handle < Array > arr = NanNew < Array > (cnt);

for (int i = 0;
i < cnt;
i++
)
{
    arr - > Set(i, Device
::
    get(devs[i])
)
    ;
}
libusb_free_device_list(devs, true);
NanReturnValue(arr);
}
 */